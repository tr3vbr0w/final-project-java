public class Calculator
{
   public /*not sure if public or static*/ calc(double[] ratio, double[] npk)
   {
    double[] val = new double[3];
    
    val[0] = ratio[0]/npk[0];
    val[1] = ratio[1]/npk[1];
    val[2] = ratio[2]/npk[2];
    
    return //not sure what to have this return as;
   }   
  
}